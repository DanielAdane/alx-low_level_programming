#include "holberton.h"
/**
 * mul - function that multiplies two integers.
 * @a: is a number input
 * @b: is a number input
 * Return: 0
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
